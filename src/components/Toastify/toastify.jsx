import { toast } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

export const notifyAdd = () => toast('Contact added!');
export const notifyDelete = () => toast('Contact deleted!');
